Use the project-manager MCP tools to inspect the current task database.

Produce a concise summary containing:

1. Total tasks
2. Tasks grouped by status
3. Tasks grouped by priority
4. Any critical or high-priority unfinished work
5. The next task you would recommend working on

Use `list_tasks` rather than reading the SQLite database directly.

Do not modify any tasks.
